# 簿記3級 Answer Sheet — 引き継ぎ資料

作成日：2026-08-21 ／ 作成：Claude（Cowork）

## 1. プロジェクト概要

簿記3級の学習用Webアプリ。ChatGPTなど外部AIに問題文・問題JSONを作らせ、このアプリで回答・採点する運用を想定。**index.html 1ファイル完結**（HTML / CSS / Vanilla JS、外部ライブラリなし、ビルド不要、npm不要、バックエンドなし）。ブラウザで直接開けば動く。データ保存はすべてlocalStorage。

## 2. 成果物ファイル

| ファイル | 内容 |
| --- | --- |
| `index.html` | アプリ本体（唯一のプロダクションファイル。約1,600行） |
| `問題JSON仕様と作成依頼プロンプト.md` | 問題JSONの仕様書＋AIへの問題作成依頼プロンプト集 |
| `test.js` | 既存機能の回帰テスト（Playwright、33項目） |
| `test-training.js` | トレーニング機能のテスト（41項目） |
| `test-blankcells.js` | blankCells仕様のテスト（19項目） |

テスト実行は `node test.js` など（Playwright＋Chromiumが必要。開発時はすべて全通過を確認済み）。

## 3. 実装済み機能

### タブ構成：大問2 ／ 大問3 ／ トレーニング

**大問2**（回答形式をセレクタで切替）
- 汎用表：行数・列数可変（縮小しても入力値は内部保持）
- 仕訳入力：借方科目／借方金額／貸方科目／貸方金額、行の追加・削除
- 勘定記入：勘定科目名＋借方・貸方の左右テーブル（日付・摘要・金額）
- 短答・空欄入力：回答欄の追加・削除

**大問3**
- 「勘定科目｜借方｜貸方」の表。代表的な17科目を初期投入、科目名編集可、行の追加・削除

**トレーニング**（穴埋め学習）
- 問題JSONインポート（モーダル貼り付け。不正JSONはエラー表示のみで画面は壊れない。```コードフェンス自動除去）
- JSONテンプレートモーダル：テンプレ表示・コピー、サンプル問題読込、**AI依頼プロンプトのコピー**
- `blankCells`指定があればそのセルをすべて回答欄に、なければランダム1穴で開始
- 「穴の数」セレクタ（1/3/5/10/すべて）＋「ランダム出題」、「次の1穴」（直前の穴を回避）。内部APIは `setRandomBlanks(count)`
- セルクリックで正解表示⇔回答欄をトグル（回答欄は黄色背景、「解」ボタンで戻す）
- 答え合わせ：穴ごとに独立して ○／×／未 判定。×でも正解は隠したまま、「正解を見る」で開示
- 「今回の問題：Nか所」表示、「問題を削除」ボタン

**共通**
- 自動保存：全入力を即時localStorageへ（キー：`boki3_answer_q2_v1` / `boki3_answer_q3_v1` / `boki3_training_v1` / `boki3_answer_ui_v1`）。リロードでタブ・回答形式・穴の位置・回答まで復元
- 「AI採点用にコピー」：形式に応じたMarkdownをクリップボードへ（空欄は「未回答」、clipboard API不可時はexecCommandフォールバック）
- 「回答をリセット」：confirmダイアログ付き（大問2は表示中の形式のみ）
- 金額欄：フォーカス中は生値、blurで100,000形式表示（内部は生値保持。数字以外はそのまま）
- キーボード移動：Enter/↑↓、文字端で←→。トレーニング表は回答欄だけを出現順に移動
- スマホでも崩れない（表はコンテナ内横スクロール）

## 4. 問題JSONの仕様（正式版）

```json
{
  "title": "問題タイトル",
  "category": "question2 または question3",
  "description": "指示文",
  "questionText": "問題文・決算整理事項（\n で改行）",
  "columns": ["勘定科目", "借方", "貸方"],
  "rows": [["現金", "100000", ""], ["貸倒引当金", "", "4800"]],
  "blankCells": [[2, 2], [4, 2]]
}
```

- `rows` は常に**完成された模範解答**。穴のために正解値を書き換えない・`"__BLANK__"`等の特殊値は使わない
- 金額はカンマなし半角数字の文字列。値がないセルは `""`（`""`セルは穴にできない）
- `blankCells` は `[行, 列]`（0始まり）の配列。個数無制限。**明示指定＞ランダム穴あけ**。なし・空配列ならランダム1穴
- 範囲外・正解が空のセル・重複は取込時に自動除外
- 許容される揺れ：コードフェンス付き、金額が数値型、`rows`が`{label, debit, credit}`オブジェクト配列（columns省略時は勘定科目/借方/貸方）

## 5. コード構造のメモ（index.html内）

- 状態はプレーンオブジェクト（`q2` / `q3` / `tr` / `ui`）＋ `loadState/saveState`。セル入力はstate直接更新（再描画なし）、構造変更時のみ再render
- 主要関数：`renderQ2Body` `renderQ3` `renderTraining` / `normalizeQuestion`（JSON検証・正規化、失敗時throw） / `applyQuestion`（blankCells優先→ランダム1穴） / `setRandomBlanks(count)` / `gradeTraining` / `buildQ2Text` `buildQ3Text`（コピー文生成） / `makeAmtInput`（金額フォーマット） / `normAnswer`（採点用正規化：全角→半角・カンマ空白除去）
- 採点比較は `normAnswer(user) === normAnswer(correct)`（100000 と 100,000 と １００，０００ は同一視）
- AI依頼プロンプトは定数 `TR_PROMPT_TEXT`、サンプル問題は `TR_SAMPLE`

## 6. GitHub Pagesへのデプロイ（2026-08-21 完了）

公開URL：**https://takatama-ts.github.io/boki3-training/**
リポジトリ：**https://github.com/takatama-ts/boki3-training**（Public）

- ローカル作業ディレクトリ：`/Users/k.takatama_ts/Projects/meguru_coin/boki3-training/`
- 配信設定：Deploy from a branch（`main` / root）、HTTPS強制。`index.html` はビルドなしでそのまま配信される
- 更新手順：ファイルを編集して `git add -A && git commit -m "..." && git push`。push後1〜2分で反映される

経緯（過去の中断理由・記録として保持）：当初の環境ではgithub.comへのHTTPSが403、その後注入された `GITHUB_TOKEN` もセッションに紐付いたリポジトリ限定でリポジトリ新規作成ができず中断していた。今回はローカルの `gh` CLI（takatama-tsアカウントで認証済み）からリポジトリ作成・push・`POST /repos/{owner}/{repo}/pages` によるPages有効化まで実施した。

注意点：無料PagesはPublicリポジトリ必須（回答データはlocalStorageなので公開されない）。localStorageはドメイン×ブラウザ単位のため、file://時代のデータはPages版に引き継がれず、端末間同期もない。URLが同じなら以後の更新でデータは消えない。

## 7. 今後の改善候補（ユーザーと合意済みではない）

- 仕訳・大問3の最終行Enterで自動行追加、貸借合計のリアルタイム検算表示
- 問題文メモ欄（AI採点用コピーに問題＋回答をセットで含める）
- トレーニングの正解数カウンター、問題の複数ストック、正解時の自動「次の1穴」
- 精算表・損益計算書・貸借対照表の専用フォーマット（大問3は将来拡張を想定した構成のまま）
