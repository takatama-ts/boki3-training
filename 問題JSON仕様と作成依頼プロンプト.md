# 簿記3級 Answer Sheet — 問題JSON仕様 & 問題作成依頼プロンプト

トレーニングタブで読み込める問題JSONの仕様と、ChatGPTなどのAIにそのまま貼って問題を作ってもらうためのプロンプト集です。

---

## 1. 問題JSONの仕様

```json
{
  "title": "大問3 決算問題 01",
  "category": "question3",
  "description": "決算整理後の残高にもとづき、表の金額を完成させなさい。",
  "questionText": "決算整理事項：\n1. 売掛金の期末残高に対して2%の貸倒引当金を設定する（差額補充法）。\n2. 備品（取得原価300,000円・耐用年数5年・残存価額ゼロ）について定額法により減価償却を行う。",
  "columns": ["勘定科目", "借方", "貸方"],
  "rows": [
    ["現金", "100000", ""],
    ["売掛金", "250000", ""],
    ["貸倒引当金", "", "5000"],
    ["備品", "300000", ""],
    ["減価償却累計額", "", "60000"],
    ["買掛金", "", "180000"],
    ["資本金", "", "318000"],
    ["売上", "", "500000"],
    ["仕入", "350000", ""],
    ["減価償却費", "60000", ""],
    ["貸倒引当金繰入", "3000", ""]
  ],
  "blankCells": [[2, 2], [4, 2]]
}
```

### フィールド

| フィールド | 必須 | 内容 |
| --- | --- | --- |
| `title` | 任意 | 問題タイトル（省略時は「無題の問題」） |
| `category` | 任意 | `"question2"` または `"question3"`（画面にバッジ表示されるだけなので他の文字列でも可） |
| `description` | 任意 | 指示文（「〜を完成させなさい」など） |
| `questionText` | 任意 | 問題文・決算整理事項など。改行は `\n` |
| `columns` | ほぼ必須 | 列名の配列。表の形式は自由（下のバリエーション参照） |
| `rows` | 必須 | 行ごとのセル値の配列。**完成された正解（模範解答）** を入れる |
| `blankCells` | 任意 | 開始時に回答欄として隠すセルを `[行番号, 列番号]`（**0始まり**）の配列で指定。例 `[[2, 2], [4, 2], [12, 1]]` なら3か所すべてが同時に回答欄になる。個数に制限なし |

### ルール

- セル値はすべて **文字列** にする。金額は **カンマなしの半角数字**（例 `"100000"`）。
- 値が入らないセルは `""` にする。`""` のセルは穴（回答対象）にならない。
- 逆に、**空でないセルはすべて穴の候補になる**。連番など解答させたくない値はセルに入れず、`questionText` 側に書く。
- `rows` は常に完成形。**穴を表現するために正解値を書き換えたり `"__BLANK__"` のような特殊値を使ったりしない**。どこを隠すかは `blankCells` だけで指定する。
- 「正解データ」「どこを穴にするか」「ユーザーの回答」はアプリ側で分離管理される。

### インポート時の優先順位

1. `blankCells` が1件以上ある → **指定されたセルをすべて回答欄にする**（ランダム穴あけはしない）
2. `blankCells` がない・空配列 → 従来どおりランダム1穴で開始

つまり **blankCellsの明示指定 ＞ ランダム穴あけ** です。「ランダム出題」「次の1穴」ボタンを押したときだけ、blankCellsの指定を破棄して新しいランダムな穴に切り替わります。範囲外のインデックス・正解が `""` のセル・重複は自動的に除外されます（リロード後も指定した穴のまま復元されます）。

### インポート時に許容される揺れ

- \`\`\`json 〜 \`\`\` のコードフェンス付きでもそのまま貼り付け可能（自動で除去）。
- 金額が数値型（`100000`）でも文字列に変換して読み込む。
- `rows` が `{"label": "現金", "debit": "100000", "credit": ""}` 形式のオブジェクト配列でも可（columns省略時は「勘定科目/借方/貸方」になる）。

---

## 2. 基本プロンプト（大問3・財務諸表）

以下をそのままChatGPTなどに貼ってください（アプリの「JSONテンプレート」→「AI依頼プロンプトをコピー」でも同じものがコピーできます）。

```
あなたは日商簿記3級の教材作成者です。以下のJSON仕様に従って、日商簿記3級の本試験レベルの問題を1問作成してください。

【出力ルール】
- JSONのみを出力する（前後の説明文は不要）
- すべてのセル値は文字列にする。金額はカンマなしの半角数字（例 "100000"）
- rows には完成された正解（模範解答）を入れる。値が入らないセルは "" にする
- questionText に、解答に必要な情報（決算整理事項・取引の内容など）をすべて書き、表の数値と必ず整合させる
- 借方合計と貸方合計が一致するよう金額を設計し、出力前に必ず検算する
- 連番など、解答対象にする意味のない値はセルに入れない

【JSON仕様】
- title: 問題タイトル
- category: "question2" または "question3"
- description: 指示文（「〜を完成させなさい」など）
- questionText: 問題文（改行は \n）
- columns: 列名の配列（表の形式は自由。例 ["勘定科目","借方","貸方"]）
- rows: 行ごとのセル値の配列（正解の完成形。穴を表現するために正解値を書き換えたり "__BLANK__" 等の特殊値を使ったりしない）
- blankCells（任意）: 開始時に回答欄として隠すセルを [行番号, 列番号]（0始まり）の配列で指定する（例 [[2,2],[4,2]]）。questionTextの決算整理事項に対応するセルを選ぶとよい。省略するとランダムに1か所が隠される

【今回作ってほしい問題】
大問3形式：決算整理後の貸借対照表・損益計算書（または残高試算表）を完成させる問題。
columns は ["勘定科目","借方","貸方"]、行数は12行前後。

【出力例】
{
  "title": "大問3 決算問題（サンプル）",
  "category": "question3",
  "description": "決算整理後の残高にもとづき、表の金額を完成させなさい。",
  "questionText": "決算整理事項：\n1. 売掛金の期末残高に対して2%の貸倒引当金を設定する（差額補充法）。\n2. 備品（取得原価300,000円・耐用年数5年・残存価額ゼロ）について定額法により減価償却を行う。",
  "columns": ["勘定科目", "借方", "貸方"],
  "rows": [
    ["現金", "100000", ""],
    ["売掛金", "250000", ""],
    ["貸倒引当金", "", "5000"],
    ["備品", "300000", ""],
    ["減価償却累計額", "", "60000"],
    ["買掛金", "", "180000"],
    ["資本金", "", "318000"],
    ["売上", "", "500000"],
    ["仕入", "350000", ""],
    ["減価償却費", "60000", ""],
    ["貸倒引当金繰入", "3000", ""]
  ],
  "blankCells": [[2, 2]]
}
```

---

## 3. バリエーション（【今回作ってほしい問題】だけ差し替える）

基本プロンプトの【今回作ってほしい問題】の段落を、以下のいずれかに置き換えてください。

### 大問2・仕訳問題

```
【今回作ってほしい問題】
大問2形式：仕訳問題。questionTextに取引を5つ（1. 2. …と番号付きで）書き、
columns は ["借方科目","借方金額","貸方科目","貸方金額"]、rowsは取引と同じ順に1行1仕訳とする。
category は "question2"。
```

### 大問2・商品有高帳（先入先出法／移動平均法）

```
【今回作ってほしい問題】
大問2形式：商品有高帳を完成させる問題（先入先出法）。questionTextに仕入・売上の取引を日付順に書く。
columns は ["日付","摘要","受入数量","受入単価","受入金額","払出数量","払出単価","払出金額","残高数量","残高単価","残高金額"] とし、
該当のないセルは "" にする。category は "question2"。
```

### 大問2・勘定記入

```
【今回作ってほしい問題】
大問2形式：勘定記入の問題。title に対象の勘定科目名（例：支払家賃勘定）を含め、
questionTextに期中取引と決算整理を書く。
columns は ["日付","摘要","借方金額","貸方金額"] とし、行は日付順、該当のないセルは "" にする。
category は "question2"。
```

### 難易度・分量の調整（末尾に追記して使う）

```
【難易度調整】
- 行数は〇行程度にする
- 金額は〇桁以内のきりのよい数字にする
- 決算整理事項は〇個にする
```

---

## 4. 使い方の流れ

1. 上のプロンプトをChatGPTなどに貼る（アプリ内「AI依頼プロンプトをコピー」でも可）
2. 出力されたJSONをコピー
3. アプリの「トレーニング」タブ →「問題をインポート」に貼り付けて「読み込む」
4. `blankCells` 指定があればその箇所が、なければランダムな1か所が回答欄になるので、埋めて「答え合わせ」
5. 「次の1穴」で同じ問題から別のセルに挑戦、慣れたら「穴の数」を増やす

※ AIは計算ミスをすることがあります。解いていて数字が合わないと感じたら、AIに「貸借合計を検算して修正して」と返すと直ることが多いです。
